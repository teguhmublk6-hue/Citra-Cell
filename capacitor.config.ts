
import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.brimo.app',
  appName: 'Brimo App',
  webDir: 'out',
  server: {
    androidScheme: 'https'
  }
};

export default config;
